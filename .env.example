# OAuth token from your Claude account (no API key needed)
# Get this from: claude config get oauthToken
# Or copy from ~/.claude/credentials.json
ANTHROPIC_AUTH_TOKEN=

# Optional: override the default ~/.battle storage directory
# BATTLE_HOME=~/.battle
